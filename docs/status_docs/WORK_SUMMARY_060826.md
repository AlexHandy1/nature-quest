# Work Summary — 6 August 2026

## What was built

Continued TDD implementation of Slice 2 (`docs/specs/spec-tool-llm-guardrails-gbif-query-040826.md`), completing REQ-019, REQ-024, REQ-020, REQ-017, and the frontend UX change (CON-001), on top of the 5 August session's guardrails/GBIF-wrapper work. 12 commits, in order:

1. `33161ad` — Clarified REQ-020 in the spec: `distinct_id` source (`posthog.get_distinct_id()`) is decided, not an open question; narrowed the `[NEEDS INPUT]` flag to only cover dashboard join-verification.
2. `2971fb5` — `app/backend/models/query.py`: added `consent: bool = False` to `QueryRequest`.
3. `a8288b2` — Added `deepeval` dev dependency + Tier 1 taxon-resolution eval test (later reworked, see Decisions).
4. `8718db6` — **`app/backend/services/ai_observability.py`** (new): `build_client(consent, distinct_id, api_key=None, posthog_client=None)` — returns a plain `anthropic.Anthropic` when `consent=False`, or `posthog.ai.anthropic.Anthropic` (PostHog's tracking wrapper client) when `consent=True`. Lazily builds/reuses a singleton `PostHogClient` from `POSTHOG_PROJECT_TOKEN`/`POSTHOG_HOST` env vars when no client is injected. `reset()` for test isolation, wired into `tests/conftest.py`'s autouse fixtures.
5. `b15eaa6` — Wired consent/distinctId end-to-end: `services/anthropic_client.py`'s `resolve_taxon_filter()` gained `**extra_kwargs` passthrough (to carry `posthog_distinct_id` to the wrapped client's `.create()` call) and an optional `on_response` callback (to expose the raw response without changing the return contract); `build_client()` refactored to extract `resolve_api_key()`. `routers/query.py`'s `_resolve_taxon_filter()` now builds the consent-gated client via `ai_observability.build_client()`.
6. `12eb556` — Built out the REQ-024 eval suite: `tests/evals/test_taxon_resolution_eval.py` (5 cases: birds→class/Aves, plants→kingdom/Plantae, insects→class/Insecta, fungi→kingdom/Fungi, turtles→order/Testudines), `tests/evals/test_full_pipeline_eval.py` (Tier 2: real Anthropic + real GBIF, asserts non-empty species list where every species independently verifies as class Aves via a `species/match` lookup), `tests/evals/test_ai_observability_capture_eval.py` (optional, `pytest.mark.skipif` auto-skips without `POSTHOG_PROJECT_TOKEN`). **Dropped the `deepeval` dependency entirely** (see Decisions) — plain `print()` of full request/response instead.
7. `945389b` — **`app/frontend/src/lib/posthog.ts`**: added `getDistinctId()`, `hasConsent()`, `trackEvent()`, exported `CONSENT_KEY` (previously private to `ConsentBanner.tsx`, now shared).
8. `6c6a021` — **`app/frontend/src/components/QueryForm.tsx`** (new): idle → loading → one-shot-result states, built via TDD (`QueryForm.test.tsx`, 14 tests). POSTs `{query, distinctId, consent}` to `/api/query`.
9. `aa16346` — Wired `trackEvent('query_submitted')` / `trackEvent('query_outcome', {status})` into `QueryForm` (REQ-018).
10. `bf5ad1d` — `App.tsx`: replaced `InterestForm` with `QueryForm` (CON-001); `ConsentBanner.tsx` copy updated to mention query collection specifically. `InterestForm.tsx`/test left in place, unreferenced (see Decisions).
11. `29a33c0` — **REQ-017**: `services/logging_client.py` gained `log_query_outcome(query, outcome, guardrail=None, input_tokens=None, output_tokens=None, gbif_result_count=None)`. Wired into every outcome branch of `routers/query.py` (daily_limit_reached, unresolved ×2, gbif_unavailable, no_results, resolved) and into `services/rate_limiter.py`'s `handle_rate_limit_exceeded` (made `async def` to `await request.json()` for the query text — see Decisions). `routers.query._resolve_taxon_filter()` changed return shape to `(taxon_filter, usage_dict)` to carry token counts through to the log line.
12. `9b94540` — UX polish: loading state now disables the input too (not just the button) and explicitly states the form can't be edited until the search finishes; result screen adds "Refresh the page to search again."; dropped the out-of-scope `"something rare"` placeholder example (rarity/`sort` is explicitly out of scope this slice).

Also: `docs/FEATURE_IDEAS_BACKLOG.md` — added "Internal capability: agent-browser-driven end-to-end smoke tests" idea. Root `.env` — added `POSTHOG_PROJECT_TOKEN` (same value as frontend's `VITE_POSTHOG_KEY`, same PostHog project) so eval/manual testing can exercise the real capture path.

**Test status at end of session**: 52 backend tests pass (`pytest -m "not eval"`), 5 eval tests pass (`pytest -m eval`, real Anthropic + GBIF, one GBIF 503 retry needed — known flakiness, not a bug), 35 frontend tests pass (`npx vitest run`).

## What was explored / learnt

- **DeepEval's local tracing gives no actual console visibility without a Confident AI account** — contradicts what the docs implied. Verified empirically: `@observe`/`update_current_span` produced only an HTTP-log line and `"[Confident AI Trace Log] No Confident AI API key found. Skipping trace posting."`, never the actual span input/output. Checked the installed CLI directly: `deepeval view` unconditionally calls `upload_and_open_link()` (cloud-only). Also hit a real library bug: `@observe(type="tool", name=...)` crashes (`ToolSpan() got multiple values for keyword argument 'name'`) because `Observer` reads `name` out of `observe_kwargs` into `self.name` without removing it, then `ToolSpan(**span_kwargs, **self.observe_kwargs)` spreads it twice — worked around by dropping `name=` on tool-type spans, but ultimately dropped DeepEval entirely once it became clear it wasn't earning its dependency cost (no metrics ever used either — a deterministic dict-equality check doesn't need an LLM-judge metric like `GEval`).
- **PostHog's OTel-based AI Observability approach (as originally spec'd: `AnthropicInstrumentor` + `PostHogSpanProcessor`) has a real architectural mismatch with REQ-020.** Read the actual `posthog-python` GitHub examples (`examples/example-ai-anthropic/chat.py`) — `posthog.distinct_id` is set once as an OpenTelemetry `Resource` attribute at `TracerProvider` construction (process-startup-level), not settable per-request. Found and switched to `posthog.ai.anthropic.Anthropic` — a wrapper subclass of the real Anthropic client accepting `posthog_distinct_id`/`posthog_trace_id`/`posthog_privacy_mode` as **per-call** kwargs on `.messages.create()`, reading its actual source (`posthog/ai/anthropic/anthropic.py`, `posthog/ai/utils.py`) to confirm it captures `$ai_input`/`$ai_output_choices` (full sanitized request/response), tokens, latency, http status per call.
- Confirmed `posthog.ai.openai.OpenAI` also just subclasses `openai.OpenAI` and passes `**kwargs` through — meaning a future OpenRouter migration (user's stated direction, `openai.OpenAI(base_url="https://openrouter.ai/api/v1", ...)`) is a straightforward wrapper-class swap, not a re-architecture. Minor nuance: `$ai_provider` would read `"openai"` for OpenRouter calls (gateway, not origin model), but `$ai_model` still carries the real model string.
- PostHog's Python client batches events via an internal queue + background thread — `capture()` returns immediately without guaranteeing delivery; `client.flush()` blocks until the queue drains. Without it, a short-lived test process can exit before the event is actually sent. Added `ai_observability._posthog_client.flush()` to the capture eval test; user confirmed the event landed in the real PostHog dashboard.
- GBIF's `occurrence/search` returned transient 503s twice during Tier 2 eval pagination (birds, ~190 pages) — matches the documented precedent in `WORK_SUMMARY_250726.md` ("not reproduced on retry — treated as GBIF-side flakiness, not a code bug"); both times passed on retry.
- Clarified for the user (concurrency mechanics): FastAPI dispatches sync `def` routes to a background thread pool (real OS threads — why `query_budget.py` needs a real `threading.Lock`), while `async def` handlers run cooperatively on the single event-loop thread via `await`. `handle_rate_limit_exceeded` had to become `async def` specifically to `await request.json()` — slowapi's rate-limit check intercepts *before* FastAPI parses the route's `QueryRequest` body (it only reads the caller's IP via `get_remote_address`), so the body is still unconsumed and safe to read there.
- Investigated a reported "consent banner not visible" issue via `agent-browser` — confirmed via a fresh browser session (`agent-browser open http://localhost:5173/`, screenshot, computed-style check: `display: flex; visibility: visible; opacity: 1`) that the banner renders correctly. Root cause was stale `localStorage['analytics-consent']` in the user's own browser from earlier manual testing (`ConsentBanner` correctly returns `null` once a choice is stored) — confirmed by the user via incognito. Not a bug.

## Decisions and trade-offs

- **Decision:** REQ-019 built on `posthog.ai.anthropic.Anthropic` (wrapper client), not OpenTelemetry `AnthropicInstrumentor`/`PostHogSpanProcessor` as the spec originally specified. **Why:** the OTel approach's `distinct_id` is process/tracer-level (`Resource` attribute), incompatible with REQ-020's genuinely-per-request `distinct_id`; the wrapper client's `posthog_distinct_id` kwarg is per-call natively, and is less infrastructure (no `TracerProvider`/`Resource`/span-processor setup). **Trade-off:** deviates from the committed spec's REQ-019/§6 ("`AnthropicInstrumentor().instrument()` + `PostHogSpanProcessor`") and §10 (dependency list still says `opentelemetry-instrumentation-anthropic`) — **spec not yet updated, explicitly deferred to the documentation pass** (tracked as pending task #1 this session).
- **Decision:** Dropped the `deepeval` dependency entirely from the eval suite. **Why:** its tracing gave zero local visibility without a paid/cloud Confident AI account (verified, see above), a real library bug was hit, and no DeepEval metric was ever actually used (correctness is a plain deterministic dict comparison, not something needing an LLM judge). Real visibility now comes from plain `print()` of the full request (model/system prompt/tool schema/query) + response, plus PostHog's dashboard for the real production-parity trace. **Trade-off:** REQ-024's spec text ("built on DeepEval") is now stale — also deferred to the docs pass.
- **Decision:** `resolve_taxon_filter()` gained an `on_response` callback param rather than changing its return type. **Why:** preserves the existing `dict | None` contract for all other callers/tests while letting `routers/query.py` extract token usage from the raw response for REQ-017's logging.
- **Decision:** `routers.query._resolve_taxon_filter()` now returns `(taxon_filter, usage_dict)` — this one **did** change shape and required updating existing test mocks in `test_query_outcomes.py` and `test_query_rate_limit.py` (previously `return_value={"taxonRank": ...}`, now `return_value=({"taxonRank": ...}, {})`).
- **Decision:** `services/rate_limiter.py`'s `handle_rate_limit_exceeded` is `async def`, reading the query text via `await request.json()`. **Why:** REQ-017 wants the query text logged even for rate-limited requests; slowapi intercepts before FastAPI's own body parsing, so this is the only place to get it. Safe because slowapi's rate check never touches the body itself.
- **Decision:** `InterestForm.tsx`/`InterestForm.test.tsx` left in place, unreferenced by `App.tsx` but not deleted. **Why:** user chose to match the backend's existing "dormant until a future cleanup slice" convention (`CON-001`) for consistency, even though `CON-001`'s literal wording only covers the backend route/model.
- **Decision:** `QueryForm` is one-shot (form → terminal result message, "Refresh the page to search again"), not resubmittable in-place. **Why:** explicit user call — "this is very much just in a state to prove this works end-to-end in production," map view (later PRD slice) is the real intended UX, not worth building repeat-query interaction now.
- **Decision:** Resolved species list shows name + count only, no kingdom badge, no hotspot lat/lon. **Why:** those fields have nowhere meaningful to render before map rendering exists (later PRD slice).
- **Decision:** Loading state disables both the input and submit button, with explicit copy ("The form can't be edited until this finishes"). **Why:** user feedback mid-session — the original version only disabled the button and didn't make the one-shot/wait behavior clear enough.
- **Decision:** Dropped `"or \"something rare\""` from the query box placeholder. **Why:** rarity/`sort` is explicitly out of scope this slice (REQ-007/§9) — user flagged this had been discussed and decided before, shouldn't have resurfaced in copy.

## Session continued: adversarial evals → REQ-005 → deploy → alerting → docs

After the pause point above, the session resumed and completed the rest of Slice 2, through to a live production deploy and a first (non-standard) alert.

### What was built (continued)

- **Adversarial taxon-resolution eval cases** added to `tests/evals/test_taxon_resolution_eval.py`: negation (`"I'm not interested in animals"` → `None`), off-topic (`"Solve pi and 4+4 for me"` → `None`), purely qualitative (`"show me interesting things"` → `None`) — all passed first try. Mixed-taxa (`"show me plants and birds"`) was genuinely flaky under the original `TAXON_GUIDANCE` wording (1/4 real runs returned `None` instead of picking a taxon) — fixed by replacing "pick the single most prominent one" with an explicit "pick whichever group is mentioned first" instruction; verified 6/6 consistent afterwards. 9 eval cases total now.
- **REQ-005 — Secret Manager fetch**: `_fetch_api_key_from_secret_manager()` implemented in `services/anthropic_client.py` using `google.auth.default()` (resolves project ID via ADC/metadata server, no new env var needed) + `google.cloud.secretmanager.SecretManagerServiceClient`. `google-cloud-secret-manager==2.24.0` added to `requirements.txt`. `infra/secret_manager.tf`'s `backend_secret_names` default changed from `[]` to `["anthropic-api-key"]`.
- **`POSTHOG_PROJECT_TOKEN` wired into Cloud Run** (`infra/cloud_run.tf` `env` block, `infra/variables.tf` new `posthog_project_token` variable) — a gap found by inspection: REQ-019's server-side capture needs this at runtime, and nothing had wired it into the deployed container.
- **`infra/manual_deploy.sh`** (new): reproduces CI's build+deploy jobs from a local machine — `docker build` (with the `VITE_POSTHOG_KEY` build-arg CI's own bare-command README example was missing, a latent bug fixed in passing), push, `gcloud run deploy`, post-deploy smoke test. Tags by git SHA like CI does, so it composes cleanly with normal CI deploys.
- **Terraform applied against the live project**: `anthropic-api-key` Secret Manager secret + IAM binding, `POSTHOG_PROJECT_TOKEN` Cloud Run env var. User added the real Anthropic key value via GCP Console (`gcloud secrets versions add`, version 1).
- **PR #14 opened and merged to `main`** (31 commits). GitHub Actions never triggered on either the PR or the `main` merge — investigated at length (workflow `state: active`, repo `actions/permissions: enabled`, path filters matched, no rulesets, `gh` OAuth token missing `workflow` scope was tried and ruled out) before confirming via githubstatus.com: a genuine GitHub platform-wide "Partial System Outage" at the time. Deployed via `infra/manual_deploy.sh` instead — succeeded, revision `nature-quest-production-00007-fms`, smoke test passed, and a real `/api/query` request against prod confirmed the full pipeline (Secret Manager key fetch included) works end-to-end live.
- **`docs/decisions/ADR-009-posthog-ai-observability-wrapper-client.md`** (new): documents REQ-019's wrapper-client-vs-OTel decision and the DeepEval drop, with alternatives considered.
- **Spec corrections** (`spec-tool-llm-guardrails-gbif-query-040826.md`): frontmatter status/sources, REQ-019, REQ-020, REQ-024, §6, §9, §10, §13 test-strategy line all corrected with "Implementation note" callouts pointing to ADR-009, rather than rewritten wholesale — status changed from "Design complete, not yet built" to "Built and deployed."
- **`ARCHITECTURE.md` and `README.md`** rewritten to reflect the deployed state (QueryForm live, Secret Manager wired, new services, manual-deploy fallback documented).
- **A real-time per-query email alert** (user's explicit ask, mid-session, distinct from REQ-025/026/027): `infra/monitoring.tf` — `google_monitoring_notification_channel` (email), `google_logging_metric` counting `query_outcome` log lines, `google_monitoring_alert_policy` firing on any count > 0 in a 60s window (`duration = "0s"`, i.e. near-immediate, not a sustained-breach threshold). New `variables.tf` `alert_email` variable (undefaulted, `CON-003` pattern). Applied to the live project (needed one retry after a ~90s metric-propagation delay — a known GCP gotcha, not a config error). Channel verified via direct REST calls (`sendVerificationCode` + `verify`) since Terraform-created channels don't auto-trigger GCP's normal verification email flow.
- **`docs/decisions/ADR-010-realtime-per-query-alerting.md`** (new): documents this as a deliberate, explicitly-scoped short-term deviation from `GUD-003`'s "keep alerting narrow" guidance — will not scale, accepted for now given near-zero pre-launch traffic, revisit trigger noted. Also captures the three GCP gotchas hit while building it (channel auto-verify, metric propagation delay, Console project-selector mismatch).
- `infra/README.md` updated: `terraform apply` example now includes `alert_email`, condensed pointer to `ADR-010` for the GCP gotchas rather than duplicating the detail inline (trimmed down after initial draft was judged too detailed for the alert's relative importance).

### What was explored / learnt (continued)

- **`gcloud config list`'s default project (`ai-workflow-tests`) differs from the project actually being worked on (`nature-quest-504414`)** — every command this session explicitly passed `--project=nature-quest-504414` (or had it in the API URL path) so this didn't cause incorrect actions, but it's worth remembering `gcloud`'s ambient default isn't this project.
- **GCP Console pages can silently show a different project than a page's own `?project=` URL param implies** — led to a real, initially-confusing "nothing is configured" report from the user for resources that direct API queries proved did exist. The Console's top-nav project selector is the actual source of truth for what a given page renders, independent of the URL.
- **Terraform-created `google_monitoring_notification_channel` resources don't trigger GCP's automatic verification email** — that only happens via the Console UI's own "add channel" flow. Had to call `notificationChannels:sendVerificationCode` then `:verify` directly via the REST API (`gcloud alpha monitoring` isn't installed by default, wasn't used).
- **A freshly-created `google_logging_metric` isn't immediately visible to `google_monitoring_alert_policy`'s API** — a ~1-2 minute propagation delay causing a 404 "Cannot find metric(s)" on first `terraform apply`; resolved by retrying (via a background `until`-loop), not by changing anything.
- GitHub Actions outage investigation used only what was available without extra permissions (`gh api actions/permissions`, `actions/workflows`, `actions/runs`, `check-suites`, repo `rulesets`) before landing on the real cause via githubstatus.com's public status API — the check-suites-for-the-merge-commit query (`total_count: 0`) was the most diagnostic single signal: it meant GitHub never even evaluated the push, not that it evaluated and skipped.

### Decisions and trade-offs (continued)

- **Decision:** `resolve_api_key()`'s Secret Manager branch uses `google.auth.default()` to resolve the GCP project ID rather than a new env var. **Why:** works automatically via Application Default Credentials on Cloud Run (metadata server), avoiding another platform-specific config value to wire through Terraform.
- **Decision:** Deployed manually via `infra/manual_deploy.sh` rather than waiting out the GitHub incident. **Why:** user's explicit call once the outage was confirmed externally (not a repo/config problem) — no reason to block on a third-party incident when a documented, safe fallback already existed (the project's pre-existing manual-deploy README section, formalized into a script this session).
- **Decision:** Built a real-time per-query email alert (`ADR-010`) ahead of REQ-025/026/027's spec'd uptime/5xx alerting. **Why:** explicit user want — immediate visibility into product usage while validating the live app, not site-health monitoring. **Trade-off:** explicitly acknowledged as non-scaling (no batching/digest), a temporary pre-launch choice, not the long-term alerting posture; REQ-025/026/027 remain the next alerting work.
- **Decision:** Spec corrections done as targeted "Implementation note" callouts at the relevant REQ/section lines, not a wholesale rewrite of the original design text. **Why:** matches this project's own established convention (already used in `ADR-007`'s "Implementation note (date):" additions) — preserves the original design intent as history while making the current, correct state unambiguous at the point a reader would look for it.

## Next steps

1. **REQ-025/026/027 — proper uptime + 5xx-rate alerting.** Still not built; the per-query alert (`ADR-010`) is an explicitly temporary stand-in, not a replacement.
2. **Revisit/replace the per-query alert before real launch traffic** — either remove it or convert to the daily-digest idea already in `docs/FEATURE_IDEAS_BACKLOG.md`, once it stops being useful and starts being noise.
3. **Push the local branch and confirm GitHub Actions is healthy again** before relying on it for future deploys — user explicitly held off pushing pending outage confirmation; not yet pushed as of this summary.
4. LICENSE choice — still `[NEEDS INPUT]`, carried over across multiple sessions, still blocking.
5. REQ-020's PostHog dashboard-join verification — still worth a quick manual check now that real production traffic with `distinct_id` exists.

**Task list state at end of session**: all 13 tracked tasks (#1–#13) completed.
