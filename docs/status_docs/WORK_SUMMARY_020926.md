# Work Summary — 02 September 2026

## What was built

`docs/specs/spec-architecture-openrouter-taxon-resolution-280826.md` implemented in full, via Claude Code on the web running autonomously in a cloud session (branch `build_openrouter_for_taxon_resolution`), then verified and documented locally.

- **`services/openrouter_taxon_client.py`** (new) — model-agnostic OpenRouter taxon-resolution client, `MODEL = "google/gemini-3.7-flash"` as the sole model-identity constant, imports `TAXON_GUIDANCE`/`QUERY_SCHEMA_TOOL` from `services/anthropic_client.py` rather than duplicating them.
- **`services/ai_observability.py`** — added `build_openrouter_client()`, same consent-gated shape as the existing Anthropic branch, pointed at `https://openrouter.ai/api/v1`. Anthropic branch untouched (still used by `narration.py`).
- **`routers/query.py`** — taxon-resolution call site swapped to the OpenRouter client; usage capture reads `.prompt_tokens`/`.completion_tokens`.
- **`services/anthropic_client.py`**'s `TAXON_GUIDANCE`** — one new unconditional rule added: Testudines is always `class` rank, never `order`, closing the one miss (24/25) found during prototyping (`WORK_SUMMARY_280826.md`).
- Unit tests rewritten to the OpenAI response shape; `anthropic_client.resolve_taxon_filters` kept covered by its own Anthropic-shaped test (REQ-015 rollback path).
- `openai==2.53.0` added to `requirements.txt`.
- **`ARCHITECTURE.md`** updated — new component, request-flow description, and a note on the unpinned provider-routing gotcha (see below).
- **`docs/decisions/ADR-015-openrouter-gemini-taxon-resolution-provider-swap.md`** (new) — records the provider-swap decision, alternatives considered, and the real-cost correction found during this session.
- Spec's `status:` frontmatter updated to "Built," pointing at ADR-015 for the cost-figure deviation.
- **`docs/status_docs/claude_web_auto_prompt_example_for_openrouter_build_020926.md`** (new) — the actual prompt used to kick off the cloud session, plus the environment-setup caveats worked through live (see below), kept as a reusable template.

Result at session end: `pytest -m eval tests/evals/test_taxon_resolution_eval.py` 25/25 (confirmed by the cloud session); `pytest -m eval tests/evals/test_full_pipeline_eval.py` 4/4 (confirmed locally — see dead end below). Local manual UI test done (draw area, real query, real walk returned); PostHog `$ai_generation` event confirmed showing in the dashboard for this call site.

## What was explored / learnt

- **Claude Code web (cloud sessions) needs explicit environment setup for this kind of live-API-dependent spec** — no network egress or secrets by default beyond a Trusted allowlist (npm/PyPI/GitHub/cloud SDKs), nothing to `openrouter.ai` or PostHog's ingestion host.
- **The environment's "Environment variables" box is plaintext to anyone with access to that environment** — not a secrets store; the UI says so explicitly. The docs describe a separate "API credentials" mechanism (key held by Anthropic's own proxy, attached to matching outbound requests, never readable inside the session) for exactly this case — but **it was not present in this account's environment editor** (only Allowed Domains / Environment variables / Setup script showed up, even on an already-created environment being edited, which is the documented precondition for it to appear). Fell back to a scoped, spend-capped OpenRouter API key placed directly in the plain env var box, to be revoked after the session.
- **Real bug hit from that fallback**: switching the environment's network access to **Custom** to allow `openrouter.ai`/PostHog's host silently dropped the **Trusted** defaults (`pypi.org`, `files.pythonhosted.org`, etc.) — Custom mode replaces the default allowlist unless "Also include default list of common package managers" is also checked. First cloud-session attempt stalled and failed on `pip install openai==2.53.0` with a 403 from `pypi.org` as a direct result. Confirmed the fix isn't hot-reloaded into an already-running session (network policy is baked in at VM provisioning) — the stalled session had to be abandoned, not retried, and a fresh session started against the corrected environment.
- **`POSTHOG_PROJECT_TOKEN` and a live smoke test of `POST /api/query` were deliberately excluded from the cloud session's task** — the PostHog live-capture eval case already skips cleanly without the token (`REQUIRES_POSTHOG` guard), and browser automation isn't expected to work in that sandbox. Both were done manually, locally, afterward instead.
- **The spec's cost rationale doesn't hold at the margin it claimed.** Ran one real query locally (`"I want to see birds"`) via the exact production client and inspected the raw OpenRouter response: `cost: $0.00096225` for 678 prompt / 121 completion tokens, provider `"Google AI Studio"`. Backed out to ~$0.75/M input, ~$3.75/M output — roughly **2x** the $0.375/$1.875 figure the spec cited from the original prototyping session, though still cheaper than Haiku's $1.00/$5.00. `openrouter_taxon_client.py` sets no explicit `provider` routing preference, so OpenRouter's default "Balanced" mode picks the tier per call (this model has three: Flex/standard/Priority, ~3.6x spread) — actual cost is not currently pinned or fully predictable. Captured as ADR-015's Consequences, not acted on (no code change made).
- **Dead end / flake investigated**: `test_full_pipeline_eval.py::test_fish_birds_and_insects_query_sorts_the_merged_result_by_count_descending` failed on the first local run (`GbifUnavailableError: GBIF occurrence/search failed after retries`) after a burst of GBIF traffic from an unrelated diagnostic script run moments before. Checked `MAX_CONCURRENT_GBIF_REQUESTS = 3` is correctly enforced in both `routers/query.py` and `services/gbif_client.py` (confirmed via code read, not assumption) — the concurrency cap held. The real gap: `gbif_client.py::_gbif_search`'s retry loop (`MAX_RETRIES = 2`) has **no backoff between attempts** — a 429 retries immediately into the same rate-limit window. Re-ran the same test in isolation a few minutes later: all 4 full-pipeline tests passed, confirming a rate-limit flake under load rather than a real regression from the OpenRouter swap. The no-backoff gap itself is pre-existing, out of scope for this spec, and not fixed this session — related to the existing `Rearchitect GBIF data dependency` item in `docs/FEATURE_IDEAS_BACKLOG.md`, which already documents the broader 429/concurrency problem but not this specific no-backoff detail.
- **Push to origin was blocked from the cloud session** — GitHub write access for that session wasn't installed (org-level 403). Worked around by continuing locally instead (the session's 8 commits were separately available locally, not re-derived) rather than trying to force push access from inside the session.

## Decisions and trade-offs

- **Decision:** Use OpenRouter's "API credentials" feature where available for spend-bearing keys in cloud environments; fall back to a scoped, spend-capped, single-use key in the plain env var box only when that feature isn't present. **Why:** the plain env var box is explicitly readable by anyone with access to the environment (product's own warning) — a spend-bearing key there is a real exposure, not a hypothetical one. **Trade-off:** the fallback still exposes the key for the session's lifetime; mitigated by capping spend and revoking after use, not eliminated.
- **Decision:** Exclude the live `POST /api/query` smoke test and PostHog dashboard confirmation from the autonomous cloud session's task; do both manually afterward. **Why:** browser automation isn't expected to work in that sandbox, and AC-002's PostHog confirmation is explicitly a manual step in the spec already. **Trade-off:** the "done" signal from the cloud session alone is incomplete — a human pass is required before the spec's Validation Criteria are actually satisfied.
- **Decision:** Document the real-cost finding as an ADR (ADR-015) plus a spec status-line update, following this repo's existing convention (ADR-009 does the same for a prior spec deviation), rather than silently accepting the corrected number or treating it as a session-only note. **Why:** the spec's headline cost-savings claim is part of its rationale (§9) and affects a future reader's or agent's trust in that number — needs to be discoverable from the spec itself, not just this dated status doc. **Trade-off:** none — no code change was made in response; provider-routing pinning is flagged as an open follow-up, not decided either way.

## Next steps

- Decide whether to pin OpenRouter provider routing (e.g. `provider: {"sort": "price"}` in `openrouter_taxon_client.py`) now that default "Balanced" routing's actual cost/tier behavior is confirmed unpredictable — currently an open question in ADR-015, not yet a code change.
- Run `ruff`/`mypy` locally once more before opening the PR (established CI-parity habit, per `WORK_SUMMARY_210826.md`).
- Push the 8 local commits and open the PR (this repo's CI only triggers on `pull_request` or `push` to `main`, confirmed by reading `.github/workflows/ci-cd.yml` — nothing fires from working the branch itself).
- Revoke/rotate the scoped OpenRouter API key used in the cloud environment now that the build is done.
- Consider adding retry backoff to `gbif_client.py::_gbif_search` at some point — not required by this spec, but the no-backoff gap was directly observed causing a real (if intermittent) eval failure this session.
