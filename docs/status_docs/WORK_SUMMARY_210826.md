# Work Summary — 21 August 2026

## What was built

Production audio-narration feature, built end-to-end via `/grill-me` → `/tdd`, then deployed:

- **`app/backend/services/wikipedia_client.py`** — `fetch_species_image` renamed to `fetch_species_summary`, now returns `{"image_url", "extract"}` instead of just the image URL. Piggybacked on the existing per-species summary fetch in `enrich_species()` (no new network call).
- **`app/backend/services/species_enrichment.py`** — `enrich_species()` output gains an `extract` field.
- **`app/backend/services/narration.py`** (new) — `generate_narrative(species_list, client, on_response=None, **extra_kwargs)`. Haiku, `GROUNDED_FACTS_GUIDANCE` prompt (ported/tightened from `narration_eval_spike.py`), `sanitize_dash_pauses()` (deterministic dash→comma swap post-generation), a `REFUSAL_SENTINEL`/`REFUSAL_GUIDANCE` content-safety mechanism (model outputs a fixed sentinel instead of a narrative when a species entry isn't a real organism or looks inappropriate; `generate_narrative` detects it deterministically and returns `None`). `on_response`/`**extra_kwargs` support added later for PostHog observability wiring. `TEMPERATURE = 1` for the narrator (full creative variety, after briefly trying 0.5 and reverting).
- **`app/backend/tests/evals/test_narration_eval.py`** (new) — ported eval harness (programmatic checks: length, no-time-refs, no-dash-pauses, mentions-all-species; LLM judge: faithfulness + habitat-claims with minor/major severity split), calling the real `generate_narrative()` (not a reimplementation), parametrized across three sample walks (`retiro_birds`, `retiro_fish_and_turtles`, `central_park_nyc`). Judge runs at `temperature=0.2` (narrator stays at 1). Includes a live test confirming the refusal guardrail actually fires against real model behavior (a crafted `NON_ORGANISM_WALK` entry, "Nuclear Warhead").
- **`app/backend/services/tts.py`** (new) — `synthesize_speech(text, api_key, voice)` via OpenRouter/Kokoro-82M (`hexgrad/kokoro-82m`, `response_format: "mp3"` — required, otherwise the endpoint returns raw headerless PCM no browser can play). `resolve_api_key()` mirrors `anthropic_client.py`'s Secret Manager pattern for `OPENROUTER_API_KEY`.
- **`app/backend/services/narration_budget.py`** (new) — separate daily call-cap counter from `query_budget.py`, same shape/cap.
- **`app/backend/models/narration.py`** (new) — `NarrateRequest`/`SpeciesInput`. Species list constrained to exactly `TOP_SPECIES_COUNT` (5). Field lengths capped (`common_name`/`species` ≤500 chars, `extract` ≤2000 chars) — added after a security review.
- **`app/backend/routers/narration.py`** (new) — `POST /api/narrate`: daily budget check → `_generate_narrative()` (consent-gated `ai_observability.build_client()`, mirrors `routers/query.py`'s `_resolve_taxon_filters` exactly, including `posthog_distinct_id` passthrough and token-usage capture) → refusal check (422 `narration_declined`, no TTS call) → TTS synthesis (502 `tts_unavailable` on provider failure) → `{narrative, audio: base64}`. Rate-limited same as `/api/query`.
- **`app/backend/services/logging_client.py`** — new `log_narration_outcome()`.
- **`app/backend/services/rate_limiter.py`** — shared `handle_rate_limit_exceeded` now branches by `request.url.path` so a rate-limited `/api/narrate` call logs `narration_outcome` instead of a misleading `query_outcome` with a null query.
- **`app/backend/tests/evals/test_narrate_audio_eval.py`** (new) — scoped live eval for the narrative→audio gap specifically: generates a real narrative then real audio, asserts MP3 magic-byte header and a plausible byte-size range (10KB–5MB). Confirmed live: 920-char narrative → 443KB mp3.
- **Frontend**: `app/frontend/src/components/NarrationControl.tsx` (new) — inline control in `ResultsPanel`'s header row next to "Your walk" (wraps to its own line on mobile via `flex-basis: 100%`, matching the existing `nav-bar__message` pattern). Cycles idle ("Create narrative") → loading → play/pause once ready. Transcript hidden behind a separate "Show transcript" toggle. `MapView.tsx`'s `Species` type gained `extract`; `distinctId` threaded down through `ResultsPanel` → `NarrationControl`.
- **`docs/decisions/ADR-014-audio-narration-data-flow-and-response-architecture.md`** (new) — stateless data flow, synchronous single-call response shape, separate daily budget, content-safety guardrail (described at category level per the security-doc redaction rule), and the alternatives considered for each (backend caching, two-step endpoint, object storage/streaming, shared budget).
- **`ARCHITECTURE.md`** — updated: overview paragraph, new backend services/router/model entries, new frontend component entry, eval-tier description extended, infra Secret Manager line (`openrouter-api-key` added), request-flow diagram gained the `/api/narrate` branch.
- **Infrastructure**: `infra/secret_manager.tf` — `openrouter-api-key` added to `backend_secret_names`. Applied live (see below).
- **PR #23 opened, fixed, and merged to `main`.** CI's `unit-tests-backend` job failed on first push (see below); fixed and re-pushed, all checks passed, merged. Feature is live in production.
- **`app/backend/routers/narration.py`/`tests/test_narrate.py`** — `_resolve_openrouter_api_key()` return type fixed from `str | None` to `str` (raises `RuntimeError` if no key resolves, caught by the existing `tts_unavailable` path) — found via running `ruff`/`mypy` locally before pushing, since CI runs both. Two tests updated to mock `_resolve_openrouter_api_key` directly rather than relying on a real key being present via the local `.env` (see below).
- **`app/frontend/src/components/ResultsPanel.tsx`** — `NarrationControl` now gets `key={species.map((s) => s.species).join('|')}`, on branch `hotfix_narrator_gen_state` (off `main`, post-merge), shipped. Fixes narration state (transcript/audio) persisting across a new search — see diagnosis below.

## What was explored / learnt

- **Judge non-determinism is real, not just narrator non-determinism.** Same evidence (e.g. "Western Palearctic" named verbatim from the extract), different runs of the judge gave opposite faithfulness verdicts. Set judge `temperature=0.2` (down from unset/default 1) to reduce this, narrator stays at `temperature=1` for creative variety per explicit request.
- **Judge prompt gaps found via live runs, not speculation**: "this precious habitat" (vague, unnamed) was wrongly flagged as a specific habitat claim — fixed by clarifying the judge tool's `unsupported_habitat_claims` description to exclude generic unnamed words. "buildings and open spaces" (a general description of the walk's own visible setting) was also wrongly flagged — fixed by extending the same carve-out to cover general-setting descriptions, not just place names.
- **Generator gaps found via live runs**: a run summarized 5 species down to essentially 1 (only the carp) — fixed by adding an explicit "mention every one of the N species by name" instruction to the prompt. "Worldwide"-style range overgeneralization (extract says "temperate Americas, Eurasia, North Africa", narrative said "worldwide") persisted even after one round of tightening — a second, more explicit ban on unqualified absolute-scope words was added, alongside a related but distinct "don't narrow a wide range down to sound local" instruction (`native to this very region` vs. the extract's actual `Western Palearctic`).
- **Near-miss: unmocked tests can make live paid API calls.** Three new field-length-validation tests were written referencing "422" as the expected status but without mocking `generate_narrative`/`synthesize_speech` — since no `max_length` constraint existed yet at that point, the request wasn't rejected at the Pydantic layer and the tests likely hit the real Anthropic API (getting 422 back by coincidence, probably the refusal guardrail firing on gibberish `"aaa...a"` input) at genuine cost. Caught immediately after the fact by noticing the test run passed *before* any implementation change existed. Fixed by adding the `max_length` constraints (making Pydantic reject before the route handler ever runs) and rewriting the tests to assert `mock_generate.assert_not_called()`/`mock_tts.assert_not_called()`, so a future regression would fail loudly rather than silently re-triggering live calls.
- **Terraform drift discovered live during `terraform plan`**: the Cloud Run service had `client = "cloud-console"` and an explicit `scaling { manual_instance_count = 0, min_instance_count = 0 }` block not matching the declared config — from a manual Console edit. User confirmed this is a recurring, known discrepancy (intended `max_instance_count = 2`). Applied in full (not scoped/targeted) per explicit instruction.
- **`terraform apply` hit a 409** — the `openrouter-api-key` secret already existed in GCP (created via Console when the user added the secret value), so Terraform's `google_secret_manager_secret.backend["openrouter-api-key"]` create step conflicted. Resolved via `terraform import 'google_secret_manager_secret.backend["openrouter-api-key"]' projects/nature-quest-504414/secrets/openrouter-api-key`, then re-planned and applied cleanly (added the IAM binding only). Confirmed for the user: importing this resource type pulls in no secret material — `google_secret_manager_secret` is metadata-only; the actual value lives in a separate `google_secret_manager_secret_version` resource never declared in this Terraform config at all.
- **Species dict field-name mismatch caught before wiring the router**: `generate_narrative()` was initially built against `lat`/`lon`, but production species dicts use `hotspot_lat`/`hotspot_lon` (`gbif_client.py:248-249`). Caught by checking `gbif_client.py` directly before assuming the shape, fixed across `services/narration.py`, `tests/test_narration.py`, and the eval sample-walk fixtures before the frontend/router work that depended on it.
- **CI failure not caught by local testing**: `test_resolved_narration_returns_narrative_and_base64_audio`/`test_tts_failure_returns_502` passed locally but failed in CI (`assert 502 == 200`) — they didn't mock API-key resolution, so they only worked locally because a real `OPENROUTER_API_KEY` happened to be present via `.env` (loaded by `main.py`'s `load_dotenv()`); CI has no `.env` at all. Fixed by patching `routers.narration._resolve_openrouter_api_key` directly in both tests, making them independent of real environment state entirely (not just shell env — `env -u OPENROUTER_API_KEY` alone wasn't a valid local repro since `.env` still supplies it either way).
- **Narration state persisted across searches — two compounding bugs, not one.** (1) `NarrationControl` had no `key` in `ResultsPanel.tsx`, so React reused the same component instance across a new search's re-render — its internal `useState` (`narrative`/`audioUrl`/`status`) never reset just because the `species` prop changed. (2) Independent of that, the button itself had no way back to `'idle'` once `status === 'ready'` — the render logic only shows "Create narrative" when `status !== 'ready'`, permanently replaced by Play/Pause after the first successful generation, so even a user manually wanting to regenerate had no affordance to do so. Fixed via `key={species.map((s) => s.species).join('|')}` (forces a clean remount per walk) rather than an internal `useEffect` reset — the `key` approach also avoids a stale-response race that an in-place `useEffect` reset would otherwise need an `AbortController` to guard against (an old in-flight fetch resolving into an unmounted instance is a safe no-op in React 18+, versus resolving into a freshly-reset live instance and clobbering it).
- **`vi.stubGlobal('URL', {...URL, createObjectURL: ...})` is a dangerous stub pattern** if anything in the same test file calls `new URL(...)` — spreading `{...URL}` copies only URL's own enumerable properties into a plain object, losing constructor behavior. Broke `ResultsPanel.tsx`'s `isTrustedImageUrl()` (which does `new URL(url)`) when reused from `NarrationControl.test.tsx`'s pattern into `ResultsPanel.test.tsx`. Fixed by assigning directly to the real class instead: `URL.createObjectURL = vi.fn(...)`, which only adds the missing method without replacing the global binding.

## Decisions and trade-offs

- **Decision:** Wikipedia extract fetched in `enrich_species()` (piggybacked, free) but carried to the narration step via a frontend round-trip, not backend caching. **Why:** the app has no session/cache layer anywhere else; `enrich_species()` runs unconditionally on every query result while narration is optional, so backend caching would mean holding state for walks that likely never trigger narration. **Trade-off:** narration's request payload is a bit heavier (species objects carry extract text). See ADR-014.
- **Decision:** `POST /api/narrate` does narrative generation + TTS synthesis synchronously in one call, returns `{narrative, audio: base64}` — no separate audio-fetch endpoint, no object storage, no streaming. **Why:** the intended UI (one button, loading → play, audio ready before play is even offered) makes a two-step design add complexity without a real UX gain; object storage/streaming solve problems this ephemeral, no-accounts, single-session app doesn't have. See ADR-014 for the full alternatives comparison.
- **Decision:** Narration gets its own separate daily budget counter (`narration_budget.py`), same rate-limit mechanism as `/api/query`. **Why:** materially different, higher cost profile per call (narrative + TTS vs. one small classification call); narration is explicitly secondary, so a burst there shouldn't be able to exhaust the core query flow's budget.
- **Decision:** Abuse mitigation for inappropriate/non-organism content uses a prompt-level refusal mechanism at the generation step (model outputs a fixed marker instead of a narrative; code detects it deterministically), not a second LLM moderation call, not GBIF re-validation of client-submitted species data. **Why:** explicit user preference — avoid adding another non-deterministic check step to evaluate/protect separately; lean on the model's own alignment, reinforced by explicit prompt instruction, with deterministic detection on the code side. Confirmed working live against a crafted non-organism test case. **Trade-off:** this is a mitigation, not a deterministic guarantee — a sufficiently crafted prompt-injection attempt could theoretically still defeat it, since `/api/narrate` accepts client-submitted species data directly with no GBIF/Wikipedia re-validation. Recorded as an accepted residual risk in both the security review output and ADR-014, deliberately deferred (the stronger fix — re-fetching `common_name`/`extract` server-side and discarding client values — was discussed and explicitly not built this session).
- **Decision:** `OPENROUTER_API_KEY` follows the exact same explicit Secret Manager client-library fetch pattern as `ANTHROPIC_API_KEY` (ADR-006/REQ-005), not Cloud Run's simpler native env-var injection. **Why:** this is an open-source repo — the existing rationale (an explicitly-fetched secret can't be accidentally swept into a wholesale `os.environ` log dump the way a plain env var could; matters more for unknown future external contributors than a single maintainer) applies identically to the new key.
- **Decision:** PostHog AI observability wiring for narration mirrors `routers/query.py`'s `_resolve_taxon_filters` pattern exactly (`ai_observability.build_client()`, `posthog_distinct_id` extra kwarg, `on_response` usage capture, tokens logged alongside outcome). **Why:** explicit ask to route narration traces into the same observability system "along with the original queries" — this pattern *is* that mechanism, not separate scope from it.
- **Decision:** Security review's one actionable finding (unbounded `SpeciesInput` field lengths) fixed with `common_name`/`species` capped at 500 chars, `extract` at 2000 chars — generous headroom (~4x the largest real Wikipedia extract seen this session, ~350-500 chars) rather than tight limits, per explicit request ("still quite small and at least is some cap").
- **Decision:** Deterministic GBIF species-name validation (re-validating a submitted scientific name resolves to a real GBIF species before generating) was discussed as a second abuse guardrail but **not built this session** — user judged the refusal guardrail sufficient for now.

## Blockers

- **OpenRouter/Kokoro-82M TTS reportedly degraded in production** — user reported audio "not generating consistently" (was working, now intermittent) shortly after the feature went live. Not yet diagnosed — session ended before pulling real evidence (attempted `gcloud auth list`/`gcloud config get-value project` to check Cloud Logging for real `narration_outcome`/`tts_unavailable` log volume, but the tool call was declined). Hypotheses not yet confirmed against evidence, in likely-order:
  1. `services/tts.py`'s own client-side `REQUEST_TIMEOUT = 60.0` being hit if the actual Kokoro-82M generation is now taking longer server-side than before (would show as intermittent `httpx.HTTPError` → `RuntimeError` → 502 `tts_unavailable`, not a hard outage — matches "not consistently" better than a flat failure would).
  2. OpenRouter-side provider capacity/routing changes for this specific open model (Kokoro-82M's serving backend is provider-dependent and can shift under OpenRouter's own load-balancing, outside this app's control).
  3. Increased request volume post-launch approaching/hitting rate limits on the OpenRouter account itself.
  - Next session: check Cloud Logging for `narration_outcome` entries with `outcome=tts_unavailable` (frequency, timing) to distinguish a real timeout/rate-limit pattern from something else; check OpenRouter's own dashboard/status for the Kokoro-82M model.

## Next steps

- **`hotfix_narrator_gen_state` shipped** — user confirmed pushed/merged directly.
- **Diagnose the OpenRouter TTS performance report** (see Blockers) before it affects more users.
- Frontend was visually checked by the user directly in-session (not via a driven browser tool) — confirmed fine at the time, but no automated screenshot/browser-driven check was run this session.
- Deterministic GBIF species-name validation for `/api/narrate` — explicitly deferred, not rejected; revisit if the refusal guardrail proves insufficient in practice.
- Cloud Run scaling drift (`manual_instance_count`/`min_instance_count` reverting to explicit `0` on some trigger) is a known recurring issue per the user, resurfaced again during this session's `terraform plan` — not investigated further, root cause still unknown.
- PostHog trace confirmation: wiring is built and unit-tested, and the consent/token-usage mechanics are verified, but no one has yet visually confirmed a real narration trace actually appears in the PostHog dashboard (only that `/api/query`'s equivalent, pre-existing pattern does).
