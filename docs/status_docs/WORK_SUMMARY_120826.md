# Work Summary — 12 August 2026

## What was built

- **Backend: Slice 9 (draw-your-own-area) foundation.** `models/query.py`'s `QueryRequest` gains a required `polygon: str` field (no backend default/fallback) with a `field_validator` enforcing minimum vertex count (3) and maximum bounding area (25 km², later flagged for a possible increase to 50 km² — see Next steps). `services/gbif_client.py` gained a shared `parse_polygon_vertices()` helper (reused by both the validator and the existing `polygon_centroid()`, avoiding duplicated WKT-parsing logic). `routers/query.py` now derives `center_lat`/`center_lon` per-request from `polygon_centroid(body.polygon)` instead of a fixed module-level constant, and passes `body.polygon` through to `fetch_top_species()`. `services/logging_client.py`'s `log_query_submitted()` now also logs the submitted polygon (REQ-015). All built test-first (84 backend tests passing, `ruff`/`mypy` clean).
- **Frontend: full draw-your-own-area implementation, redesigned twice.** First pass followed the spec literally (`AreaChoicePopup`, a custom `DrawConfirmBar` "Confirm Area" button, docked-bar redraw/switch actions gated behind a resolved result). Manual testing surfaced real, blocking UX problems (floating panels blocking Leaflet's own draw-toolbar controls; dead-end mode switches; the drawn polygon disappearing after a query; a redundant custom confirm button next to Leaflet's native controls). Redesigned per `docs/decisions/ADR-012-area-selection-persistent-widget-not-popup-funnel.md`: a persistent, always-visible `AreaControl` widget replaced the popup/funnel; the drawn polygon became a persistent `<Polygon>` overlay driven from `areaState` independent of the draw toolbar's mount state; the custom confirm button was deleted in favor of Leaflet-draw's own native edit/delete controls auto-confirming on `CREATED`/`EDITED` events. New pure utilities in `lib/polygon.ts` (`pointsToWkt`, `wktToPoints`, `validatePolygonPoints`, mirroring the backend's thresholds).
- **Full visual redesign.** Applied `/frontend-design`: a real `<header>` navbar (`app-shell` → `nav-bar`, normal document flow above the map — this is also what fixed the Leaflet-toolbar-blocking bug structurally, not just cosmetically) containing branding, `AreaControl`, and an inline `QueryPanel`. New "trail-guide" palette (parchment/forest/bark tokens) and typography (Fraunces display font + Work Sans body font, via Google Fonts) replacing the original purple-on-white default. `QueryPanel` simplified: dropped the visible "What would you want to see on a walk?" label (kept as `sr-only` for accessibility), renamed the submit button "Show me" → "Create walk", dropped the resolved-outcome message entirely (markers/route/results panel already communicate success), and changed the loading copy to "Generating your walk…". Widened the map's zoom-out range (`MIN_ZOOM` 14 → 2) for drawing anywhere in the world.
- **Two new project-local skills**, adapted from [brianlovin/agent-config](https://github.com/brianlovin/agent-config/tree/main) with light customisation and credited as such: `.claude/skills/simplify/SKILL.md` and `.claude/skills/security-reviewer/SKILL.md` (added a category for creative/open-source-aware attack vectors, since this repo is public). Both also copied verbatim into `~/global-code/ai-agent-knowledge-bank/skills/`, with that repo's `README.md` and `CLAUDE.md` updated to reference them (attribution table + skill-loading table) — and this project's own `CLAUDE.md` updated to match.
- **Simplify pass** run against this session's frontend changes: extracted a `layerToPoints()` helper to remove real duplication in `DrawAreaControl`'s `onCreated`/`onEdited` handlers, dropped two unnecessarily-exported constants in `lib/polygon.ts`, added a short comment explaining why `MapView`'s Leaflet path colors duplicate CSS tokens by hand (Leaflet needs JS color strings, can't read CSS custom properties).
- **Security review** run against the full backend/frontend/deploy surface. 5 findings (2 Medium, 1 Low-Medium, 2 Low) plus several positive findings (contained prompt-injection blast radius, no XSS surface, clean secrets hygiene, sound WIF-based deploy auth). Full report: `tests/.security_review_output/20260812_133720/report.md` (gitignored, not committed — see below for the finding summary since that directory isn't in git).
- **`tests/e2e_web_smoke_test.py` reworked** for the new UI (selectors/copy: `.nav-bar__query`, "Create walk", "Generating your walk"), plus a `reset_session()`/timeout hardening pass against stale `agent-browser` daemons. The draw-your-own-area and 25 km² oversized-area scenarios were attempted extensively (see below) and ultimately **removed** after repeated automation flakiness — documented in detail in a "known gaps" comment block at the end of the file. The script now covers the same scope as before Slice 9 (landing, fixed-area plants/birds, mobile, multi-taxon/GBIF-retry, unresolved), adapted for the new UI, plus one cheap addition (default area-control label check).
- **Documentation**: `ARCHITECTURE.md` rewritten for Slice 9 (also fixed a stale Slice 3 status line left over from a prior session — it said "not yet built" despite shipping before this session started); `README.md` rewritten twice — first to reflect Slice 9, then simplified further per explicit request (dropped the "Current state" and "Prototype validation" sections entirely, replaced with a terse "Finding your way around" pointer section after the deploy instructions, made the global-anywhere drawing capability prominent in the live-demo line, swapped the "something rare" example query for "I want to see fish and insects"). New `docs/decisions/ADR-012-area-selection-persistent-widget-not-popup-funnel.md`. Spec status line updated on `docs/specs/spec-tool-draw-your-own-area-100826.md` to point at ADR-012.
- **Deployed to production.** Live at https://nature-quest-production-465dsuxpnq-ew.a.run.app.

## What was explored / learnt

- **Extensive, ultimately unsuccessful automation of Leaflet-draw via `agent-browser`'s synthetic mouse events.** Manual, by-hand testing of the exact same interaction (via `agent-browser` refs/clicks in an interactive exploration) worked reliably — real polygon drawing, oversized-area rejection, and a geolocation override all confirmed working live. But driving the identical click sequence programmatically from the Python smoke-test script was flaky across many iterations: dropped vertices, "shape edges cannot cross" errors, and inconsistent results even after several rounds of fixes (longer settle delays, a verify-and-retry loop with `Escape`-based cancellation, geometry/centering changes). Root cause was never conclusively identified — plausible contributors include synthetic CDP mouse events landing in Leaflet-draw's click-vs-drag disambiguation window, and system load from a **stale `agent-browser` daemon left running for days** (confirmed and killed mid-session) degrading timing-sensitive interactions. After the user's own repeated headed test runs kept failing the same way even post-cleanup, the decision was made to cut scope rather than keep sinking time into it (see Decisions).
- **`gbif_unavailable` on drawn-area queries was investigated as a possible real bug** (asymmetric failure — fixed-Retiro queries never failed, drawn-area queries failed consistently) before concluding it was very likely GBIF rate-limiting responding to a burst of automated requests (5 real GBIF calls fired in ~15-20s by the script, far faster than any human), not a polygon-handling defect — corroborated by the user's own manual testing working fine. Winding-order/invalid-WKT was considered and set aside once the user reported manual success, since a structural geometry bug would have failed for them too.
- **Diagnosed and fixed a real operational issue mid-session**: a uvicorn backend process had been started with the wrong working directory (repo root instead of `app/backend`), causing "Could not import module main" and pinning ~88% CPU; restarted correctly. Separately diagnosed and killed two `agent-browser` daemon processes that had been running since the prior Monday and Thursday, plus their spawned Chrome-for-Testing processes — a likely contributor to "suddenly very slow" system behavior reported mid-session.
- Confirmed via live `curl`-equivalent testing that `agent-browser`'s `set geo <lat> <lon>` + `mouse move/down/up` primitives are sufficient in principle to drive geolocation override and polygon drawing — the capability gap was reliability/timing, not missing functionality.

## Decisions and trade-offs

- **Decision:** Area selection redesigned from a popup/funnel + custom confirm button to a persistent `AreaControl` widget + Leaflet-native auto-confirm. **Why:** manual testing found the spec's original design (built faithfully first) had multiple real, user-blocking problems. **Trade-off:** the frontend now diverges materially from `spec-tool-draw-your-own-area-100826.md`'s written state machine (REQ-005 through REQ-009) — see ADR-012 for the full reasoning, alternatives considered, and consequences.
- **Decision:** No automated end-to-end coverage for the draw-your-own-area flow (drawing, oversized-area rejection, geolocation, redraw, mode-switching) in this slice. **Why:** the user's explicit call after ~an hour of repeated automation failures on already-manually-validated functionality — "let's not get blocked on this smoke test step." **Trade-off:** this is a real, accepted coverage gap for a slice whose core feature is exactly this flow; manual verification is the only current check. Documented explicitly and in detail in `tests/e2e_web_smoke_test.py`'s trailing comment block so it isn't silently forgotten.
- **Decision:** Security review findings (global daily LLM budget, per-instance rate-limiter/budget state on Cloud Run, unverified `X-Forwarded-For` handling, permissive polygon-prefix validation, unbounded `distinctId`) were logged but not fixed this session. **Why:** user's explicit call — "this is fine and write up can be kept in that report." **Trade-off:** these remain open, real findings for a future session; full detail in `tests/.security_review_output/20260812_133720/report.md`.
- **Decision:** `simplify` and `security-reviewer` skills copied verbatim (not modified) into `ai-agent-knowledge-bank`, with attribution added crediting brianlovin/agent-config despite the light customisations already made. **Why:** explicit instruction to preserve exact text and credit the source.
- **Decision:** README simplified aggressively — no "Current state" section, no prototype-validation narrative. **Why:** explicit user request for a much simpler README; the detailed current-state narrative duplicated what `ARCHITECTURE.md` already covers in more depth.

## Next steps

Logged directly from user feedback on the live production deployment — not yet built, this is the punch list for the next slice(s).

**Priority order, explicit:** have the mobile UX discussion (below) — likely involving a few local prototypes (`/prototype`) to sanity-check direction before real implementation — **before** starting *any* of the specific mobile fixes listed under it, and **before** the results panel work. The results panel redesign should itself land within whatever mobile-friendly direction comes out of that discussion, not be built first and retrofitted.

### 0. Mobile UX discussion + prototyping (do this first, before anything below)
Current mobile UX has enough distinct problems (see items 1-5) that it warrants a focused design discussion rather than one-off patches — likely with a few throwaway local prototypes (`/prototype`) to sanity-check direction (e.g. navbar layout, geolocation affordance, icon-heavy controls) before committing to real implementation. Do this before starting items 1-9 below.

### Production issues spotted (mobile UX) — specific symptoms for that discussion, not a fix list to start from
1. **Header/navbar color inconsistency**: the header bar renders a noticeably different (lighter) brown on mobile vs. desktop — unexpected, needs investigation (likely a CSS specificity/media-query interaction, not intentional).
2. **`AreaControl`'s second row of buttons overflows the viewport** on narrow phones (reported on iPhone SE) — the "Exploring: ..." row isn't fully visible/reachable.
3. **The whole top navbar is "crumpled" and not fully visible on mobile** — hard to see and hard to use to search. The mobile viewport check added this session (`tests/e2e_web_smoke_test.py`) only verifies the map retains a minimum height and there's no horizontal overflow — it does not catch this class of layout/usability problem.
4. **Drawing on the map is hard to use**, independent of the automation flakiness noted above — this is about real end-user UX, not just test automation. May be out of scope for a "quick" slice, but flagged.
5. **Geolocation ("Use my location") is hard to access and doesn't behave as expected on mobile.** Consider leaning more heavily on icons/mobile-native patterns rather than the current text+icon button.

### Results panel / map outcome state (currently a placeholder)
6. Show **common names**, not scientific names, in the results panel.
7. **Add a picture per species** — proposed to load only when that species' marker is clicked (not eagerly for all 5), to control cost/payload.
8. **Add a link to each species' GBIF entry** so users can go elsewhere for full detail. Explicit decision already leaning this way: do **not** try to fit descriptions directly in the UI — costly to generate/maintain and unreadable on mobile. Link out instead.
9. **Consider showing all observation points (not just the hotspot) for a species when its marker is clicked** — dots for every real observation, to build user trust in the data. Flagged as possibly not mobile-friendly; pick up in the mobile UX discussion (item 0) rather than deciding now.

### Constraints / user-facing transparency
10. **Surface the query's real constraints somewhere in the UI**, subtly if possible (a click-through page is acceptable if inline isn't feasible): observations are limited to since 2023, only 5 species are returned per query, and data is sourced only from GBIF.
11. **Review raising the area cap from 25 km² to at least 50 km²** — 25 km² was called out in the original spec as "a starting guardrail, revisitable later," and may be too small for more exploratory searches.

### Two candidate directions after the above (not yet scoped/decided between)
12. **Narrative guide as audio**: use AI to generate a narrative (from selected species + possibly Wikipedia descriptive input), then a text-to-speech model — [Kokoro-82M](https://huggingface.co/hexgrad/Kokoro-82M) was named as a candidate — to produce an audio walking-guide track.
13. **LLM/AI infrastructure flexibility**: explore OpenRouter and other model providers/types to reduce exclusive reliance on Claude and improve cost efficiency, as a more infrastructure-focused alternative direction to (12).

Neither (12) nor (13) has been chosen yet — both are logged as candidate next-slice directions for a future planning session.

---

## Session 2 — Mobile UX discussion + first-pass prototype

Direct continuation of item 0 above (mobile UX discussion before any of the specific fixes/results-panel work). No production code changed this session — discussion + one throwaway prototype only.

### What was built

- **`prototypes/ux/species_detail_mobile_prototype.html`** — new standalone throwaway HTML prototype, following the exact convention of the existing `prototypes/ux/map_narrative_layout_prototype.html` (real Leaflet/OSM via CDN, hand-captured demo data, `?variant=A|B|C` switcher, deliberately loud yellow/black switcher pill). Rendered inside a fixed 375×667 phone frame so it reads as mobile without needing to resize the browser. Reuses the real app's exact trail-guide CSS tokens (`--bg #f7f1e1`, `--text-h #2c2417`, `--accent #3f6b4a`, `--border #d9c9a3`, Fraunces/Work Sans) and 5 hand-captured mock Retiro-area species (common name, scientific name, count, lat/lon, a picsum.photos placeholder image, a real `gbif.org/species/search` link). Three variants of the results/species-detail interaction model:
  - **A — Map-first bottom sheet**: peek state shows the species list (thumbnail, common name, count); tapping a row expands an inline accordion (photo, GBIF link, "show all sightings" toggle). Tapping a marker expands its row and highlights the marker.
  - **B — Bottom card carousel**: one full species-detail card at a time in a horizontal scroll-snap dock, dot indicators, active card synced to a pulsing marker highlight; tapping a marker scrolls the carousel to match.
  - **C — Marker-popup centric**: minimal chrome; tapping a marker opens a native Leaflet popup anchored to it (photo, common name, GBIF link, observations toggle inside the popup); a slim always-visible bottom chip row lets you jump between species without touching the map.
  - All three variants share the same nav bar with a **compact "Retiro Park ▾" AreaControl chip** (click-to-expand mini menu) replacing today's overflowing 3-button pill — the fix for the AreaControl mobile bug is mocked once as shared chrome, not treated as a fourth thing to compare.
  - **Deliberately kept out of `app/frontend/src`** — confirmed via `.github/workflows/ci-cd.yml` that CI (lint/typecheck/test/build/deploy) triggers on any `app/**` path change, so a prototype living there would get linted, type-checked, and built into the production bundle even if never rendered. `/prototypes/` is entirely outside those trigger paths — zero CI/CD footprint, matching this repo's established "throwaway code lives in `/prototypes/`" convention.
  - Run with `open prototypes/ux/species_detail_mobile_prototype.html` — no server, no build step. Needs internet (Leaflet/OSM tiles/Google Fonts CDNs + picsum.photos placeholder images).
  - **Not yet reviewed by the user** — built and handed over at the end of this session; review is tomorrow's first step.

### What was explored / learnt

- **Confirmed two of the five reported mobile UX symptoms as real, with root causes, via live interaction (not just code reading) against the actual local dev app** (backend on :8000, frontend on :5173, both started fresh this session):
  - **AreaControl's 3-button overflow (item 2 from the punch list) is real and reproduced live.** At a 375px viewport, after actually drawing a polygon (mouse-click sequence worked first try this session, unlike the documented flakiness from the prior session), the "Exploring: Custom area" + "Redraw area" + "Explore Retiro Park" row visibly overflows the viewport width (confirmed via screenshot, horizontal scroll visible). Root cause: `.area-control` (`index.css`) has no `max-width` or wrap behaviour of its own — `flex: 0 0 auto`, no `flex-wrap` — so its three children sit on one unbreakable row regardless of `.nav-bar`'s own `flex-wrap: wrap`.
  - **Header colour inconsistency (item 1) does NOT reproduce as a CSS bug.** Measured `.nav-bar`'s computed `background-color` via `agent-browser eval` at both mobile (375px) and desktop (1440px) widths, in both light and dark `prefers-color-scheme` — identical in all four cases (`rgb(237, 226, 201)` light, `rgb(42, 36, 24)` dark). No media query differentiates the token by viewport width. **More likely root cause: `index.html` has no `<meta name="theme-color">` tag at all** (confirmed via grep) — on mobile browsers this typically means the OS status-bar/browser-chrome colour defaults to something generic rather than matching the in-page header, which would read as "the header looks different" without any actual CSS difference in the header itself. Not yet fixed — logged as a concrete, likely-correct diagnosis for whoever picks this up next.
  - Items 4/5 (drawing hard to use, geolocation hard to access) were **not** re-verified by live interaction this session — user explicitly stopped further reconfirmation of already-known issues partway through (see Decisions below) before geolocation/touch-target measurements were taken.
- **`agent-browser`'s `open --viewport <w>x<h>` combined flag is broken in this install (v0.27.1) — silently drops network config.** Repeated `net::ERR_NAME_NOT_RESOLVED` on every navigation (production URL, `localhost:5173`, even raw `127.0.0.1:5173`) when `--viewport` was passed directly to `open`, regardless of session name, sandbox flag, or whether daemons/ports were freshly killed and restarted. Root cause isolated by elimination (killed all daemons/ports, retested each variable independently): the failure tracked exactly with presence of `--viewport` on the `open` command, not session scoping, not network state, not process staleness — an earlier hypothesis ("it's the `--session` flag") was floated and explicitly wrong, corrected after the user pushed back and asked for the actual mechanism. **Fix/workaround: use `agent-browser open <url>` (no `--viewport`) followed by a separate `agent-browser set viewport <w> <h>` call** — this works reliably. Worth remembering for any future `agent-browser` mobile-viewport work in this repo, since the failure mode (DNS-resolution-looking error) is misleading and doesn't point at the real cause.
- **Reviewed an external design skill the user pointed at**: [ceorkm/mobile-app-ui-design](https://github.com/ceorkm/mobile-app-ui-design) (`SKILL.md` + `references/industry-conventions.md`, fetched via `gh api`). Core guidance applied to this session's prototype: one font family max two (already satisfied — Fraunces/Work Sans), 8-point spacing grid, 44×44pt minimum tap targets, primary actions in the thumb zone (bottom third), 60/30/10 colour rule (existing trail-guide palette already close to this), soft/tinted shadows not harsh grey, F-pattern reading order. Per the skill's industry-conventions reference, Nature Quest doesn't map cleanly onto any one listed category (fitness/finance/crypto/etc.) — assessed as closest to a **Health/Wellness × Productivity hybrid** (calm/approachable discovery + organised/information-dense results), which argues against gradient-heavy/neon/dark-crypto treatments and toward the doc's own recommended process of presenting 3-5 structurally distinct mockups before picking a direction (informed this session's 3-variant prototype).
- **Dev servers**: backend (`uvicorn main:app --port 8000`) and frontend (`npm run dev`, port 5173) were started fresh this session from a cold state (nothing was running at session start) — left running in the background at session end.

### Decisions and trade-offs

- **Decision: stopped re-confirming already-known mobile UX issues mid-session and moved straight to design work.** **Why:** explicit user interrupt — "what i dont need you to do is re-confirm things I know are issues" — after AreaControl overflow and the theme-color hypothesis were already established; further live verification of geolocation/drawing touch-targets was cut short. **Trade-off:** items 4/5 from the original punch list remain unverified by live interaction (code-level read from the prior session still stands: Leaflet's default toolbar + the `.geolocation-prompt__button` are default/mouse-sized, not measured against the 44×44pt minimum this session).
- **Decision: rejected AI-proposed multiple-choice framing (`AskUserQuestion`) for the results-panel/AreaControl/observation-points interaction design, twice.** **Why:** explicit user pushback both times — first, "I really want us to think through the design system and prototype some ideas before we just jump into very specific things" (rejected pre-baked options before establishing that neither the map markers nor the results list currently have any click interactivity at all — confirmed by reading `MapView.tsx`/`ResultsPanel.tsx` live); second, a bare `AskUserQuestion` retry was also rejected outright. **Trade-off:** none — this was the right call; forcing a decision before the actual interaction model was understood would have anchored on the wrong things. Genuinely useful correction: **when the interaction model itself is undecided, build comparable prototypes before asking multiple-choice questions, don't ask multiple-choice questions to decide the prototype's scope.**
- **Decision: built a wide, 3-variant, high-fidelity prototype (real Leaflet, real map tiles, real interactivity — not static mockup images) rather than a narrower single-direction spike.** **Why:** explicit user ask ("show me 2-3 visual directions... for review") plus the `/prototype` skill's UI.md guidance to generate 3 *structurally* different variants, not colour/copy tweaks. **Trade-off:** larger single-file prototype (~400 lines) than a minimal spike would be; justified by the skill's explicit anti-pattern warning against variants that are too similar to be useful for a real decision.
- **Decision: kept the prototype entirely inside `/prototypes/ux/`, not `app/frontend/src`, even though the `/prototype` skill's own stated default is "sub-shape A — adjustment to an existing page" (mounted on the real route via a real `?variant=` router param).** **Why:** explicit user question ("will these get picked up in the current CI/CD process?") — checked `.github/workflows/ci-cd.yml` and confirmed CI triggers on any `app/**` path, meaning sub-shape A would have added the prototype to lint/typecheck/test/build/deploy surface area even while gated behind a dev-only check. The repo's own existing convention (`prototypes/ux/map_narrative_layout_prototype.html`) already established a zero-footprint pattern for exactly this situation. **Trade-off:** the prototype is visually/structurally standalone rather than butting up against the real app's actual React component tree — matched as closely as possible via identical CSS tokens and a real Leaflet map, but it is still a hand-copied approximation of the chrome, not the real `MapView.tsx`/`AreaControl.tsx`/`ResultsPanel.tsx` components themselves.

### Next steps

1. **User reviews `prototypes/ux/species_detail_mobile_prototype.html`** (`open` it locally, cycle through A/B/C via the switcher) and picks a direction, or names pieces of each to combine — this was handed over at the very end of this session, not yet looked at.
2. Once a direction is picked: fold the winning approach into the real components (`MapView.tsx`, `ResultsPanel.tsx`, `AreaControl.tsx`, `index.css`) as production code (TDD per usual), and capture the prototype itself as a primary source on a throwaway branch per the `/prototype` skill's capture step (not left rotting in `main`, though this repo's convention so far has been to keep `prototypes/ux/*.html` files in `main` long-term as a record — worth confirming which convention applies before cleanup).
3. **Add `<meta name="theme-color">`** to `app/frontend/index.html` (light + dark variants, matching `--surface`'s two values) as a likely fix for item 1 (header colour inconsistency) — small, independent of the results-panel direction decision, could be picked up any time.
4. **Fix `.area-control`'s overflow** (item 2/3) — now confirmed live, not just theorised. Concrete direction already prototyped (compact collapsed chip) in all 3 variants above; just needs porting to the real `AreaControl.tsx`/`index.css` once a results-panel direction is chosen (the two are being designed together, not sequentially, per the user's explicit framing this session).
5. **Items 4/5 (drawing hard to use, geolocation control) still need live-interaction re-verification** — only got as far as code-level reasoning (default/small touch targets) before the session moved to design work; worth a quick pass before or alongside implementation.
6. Everything else from Session 1's Next steps (constraints transparency, 25km²→50km² cap review) remains unstarted and unchanged.

### Housekeeping for next session

- Backend (`:8000`) and frontend (`:5173`) dev servers were left running in the background from this session — check they're still up (or restart) before continuing.
- `agent-browser` daemon was left closed (`agent-browser close --all` run near the end) — clean start next time.
